<?xml version="1.0" encoding="utf-8"?>
<set xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Scale animation to give it a smooth scaling effect -->
    <scale
        android:fromXScale="0.8"
        android:toXScale="1.0"
        android:fromYScale="0.8"
        android:toYScale="1.0"
        android:pivotX="50%"
        android:pivotY="50%"
        android:duration="300"
        android:startOffset="100" /> <!-- delay for smooth transition -->

    <!-- Translate animation with easing -->
    <translate
        android:fromXDelta="100%"
        android:toXDelta="0%"
        android:duration="600"
        android:interpolator="@android:anim/decelerate_interpolator" /> <!-- smooth easing -->

    <!-- Alpha animation for fade-in effect -->
    <alpha
        android:fromAlpha="0.0"
        android:toAlpha="1.0"
        android:duration="600"
        android:startOffset="200" /> <!-- delay to sync with translation -->
</set>
