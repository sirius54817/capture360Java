<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- Background Square with soft gradient -->
    <path
        android:fillColor="#565555"
        android:pathData="M0,0h108v108h-108z" />

    <!-- Grid lines with a modern gradient color -->
    <!-- Optional: add an icon or symbol -->
    <path
        android:fillColor="#00000000"
        android:pathData="M40,40h28v28h-28z" /> <!-- Placeholder for a square icon -->

    <!-- Optional: Text to be added as app name or slogan -->
    <!-- <text android:text="App Name" android:textSize="14sp" android:textColor="#000000"/> -->
</vector>
